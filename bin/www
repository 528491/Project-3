const server = require("../server");

server.set('port', process.env.PORT || 3001);

server.listen(server.get('port'), function(){
    console.log("Running from ./bin/www");
    console.log(`🌎  ==> API Server now listening on PORT ${server.get('port')}!`);
})