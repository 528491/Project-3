const router = require("express").Router();
const usersController = require("../../controllers/usersController");

router.route("/signup")
  .post( (req, res, next) => {
  console.log('werwe in here');

  );



module.exports = router;
